# PostgreSQL Configuration for Docker
# Copy this to backend/.env for local Docker development

# PostgreSQL Docker Configuration
DATABASE_URL=postgresql://trading_user:trading_password_2024@localhost:5432/trading_db

# Alternative individual settings (use DATABASE_URL above instead)
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=trading_db
# DB_USER=trading_user
# DB_PASSWORD=trading_password_2024

# Connection Pool Settings (optimized for local development)
DB_POOL_MIN_SIZE=3
DB_POOL_MAX_SIZE=10
DB_TIMEOUT=30.0

# Application Settings
LOG_LEVEL=INFO
ENVIRONMENT=development

# Upstox API Settings (add your actual values)
# UPSTOX_API_KEY=your_api_key_here
# UPSTOX_API_SECRET=your_api_secret_here
# UPSTOX_REDIRECT_URI=http://localhost:5173/auth/callback