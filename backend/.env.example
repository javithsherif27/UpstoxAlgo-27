# PostgreSQL Database Configuration
# Copy to .env and update values for your environment

# PostgreSQL Connection - Choose one option:

# Option 1: Full connection URL (recommended for production/AWS RDS)
DATABASE_URL=postgresql://username:password@localhost:5432/trading_db

# Option 2: Individual components (for local development)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=trading_db
DB_USER=postgres
DB_PASSWORD=your_password_here

# Connection Pool Settings
DB_POOL_MIN_SIZE=5
DB_POOL_MAX_SIZE=20
DB_TIMEOUT=30.0

# AWS RDS Example (free tier compatible):
# DATABASE_URL=postgresql://postgres:mypassword@myinstance.abc123.us-east-1.rds.amazonaws.com:5432/trading_db

# Local PostgreSQL Example:
# DATABASE_URL=postgresql://postgres:password@localhost:5432/trading_db

# For development with Docker:
# DATABASE_URL=postgresql://postgres:password@localhost:5432/trading_db